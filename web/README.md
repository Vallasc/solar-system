# How to build

- Install npm
- Install dependencies
    ```sh
    $ npm install
    ```
- Build 
    ```sh
    $ npm run build
    ```
- Run web server and hot reload
    ```sh
    $ npm run watch
    ```