{
  "name": "solar-system",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "tsc-watch": "tsc -b src/main src/dedicated-worker -w",
    "build": "tsc -b src/main src/dedicated-worker",
    "serve": "browser-sync start --server --files \"dist/*/*.js\" \"index.html\" \"css/main.css\"",
    "watch": "concurrently \"npm run tsc-watch\" \"npm run serve \""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.0.3",
    "browser-sync": "^2.26.12",
    "concurrently": "^5.3.0"
  }
}
